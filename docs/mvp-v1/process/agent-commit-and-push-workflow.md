# Legacy Mirror: Agent Commit And Push Workflow

## Document Relations

- Legacy mirror for: `docs/agent-workflow/agent-commit-and-push-workflow.md`
- Related entry point: `AGENTS.md`
- Related workflow index: `docs/agent-workflow/README.md`
- Related legacy mirror: `docs/mvp-v1/process/git-collaboration-convention.md`
- If rules change here, make the same change in the primary `docs/agent-workflow/` docs in the same update.

## Status

- The primary source of truth for agent commit and push execution steps now lives at `docs/agent-workflow/agent-commit-and-push-workflow.md`.
- Keep this file only as a legacy mirror for older MVP v1 references.

## Use Rule

- Prefer reading the primary document in `docs/agent-workflow/`.
- The primary document now separates startup reads from the pre-edit management-document gate; follow that primary sequence instead of relying on this mirror alone.
- Preserve this path while older documents still link to it.
